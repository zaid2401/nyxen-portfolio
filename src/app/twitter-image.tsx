/**
 * X/Twitter reuses the Open Graph card verbatim — one design to maintain, and
 * `summary_large_image` has the same aspect ratio.
 */
export { default, alt, size, contentType } from "./opengraph-image";
